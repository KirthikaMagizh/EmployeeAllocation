package com.coding.employee_allocation;

import org.springframework.context.annotation.Bean;


//@Configuration
//@EnableWebSecurity
public class SecurityConfig {//extends WebSecurityConfiguration {

	// Configuring HttpSecurity
	/*
	 * @Bean public SecurityFilterChain securityFilterChain(HttpSecurity http)
	 * throws Exception { http .csrf(csrf -> csrf.disable()) // Disable CSRF for
	 * simplicity .authorizeHttpRequests(auth -> auth
	 * .requestMatchers("/auth/welcome").permitAll() // Permit all access to
	 * /auth/welcome .requestMatchers("/auth/user/**").authenticated() // Require
	 * authentication for /auth/user/**
	 * .requestMatchers("/auth/admin/**").authenticated() // Require authentication
	 * for /auth/admin/** ); // Enable form-based login
	 * 
	 * return http.build(); }
	 */

	
}
